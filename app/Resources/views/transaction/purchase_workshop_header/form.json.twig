{
    "subTotal": "{{ purchaseWorkshopHeader.subTotal|default|number_format(2) }}",
    "taxNominal": "{{ purchaseWorkshopHeader.taxNominal|default|number_format(2) }}",
    "grandTotal": "{{ purchaseWorkshopHeader.grandTotal|default|number_format(2) }}",
    "purchaseWorkshopDetails": {
        {%- for index, purchaseWorkshopDetail in purchaseWorkshopHeader.purchaseWorkshopDetails -%}
            "{{ index }}": {
                "total": "{{ purchaseWorkshopDetail.total|default|number_format(2) }}"
            }
            {%- if not loop.last -%},{%- endif -%}
        {%- endfor -%}
    }
}
{% extends 'transaction/layout.html.twig' %}

{% block actions %}
    {{ include('transaction/actions.html.twig') }}
{% endblock %}

{% block content %}
    {{ form_start(form) }}
        <div class="row">
            <div class="col-sm-6">
                <div class="form-group">
                    <label>Receive Order #</label>
                    <div>{{ receiveOrder.codeNumber }}</div>
                </div>
                {{ form_row(form.transactionDate) }}
                {{ form_row(form.vehicleProductionYear) }}
                {{ form_row(form.vehicleChassisNumber) }}
                {{ form_row(form.vehicleMachineNumber) }}
                {{ form_row(form.vehicleDescription) }}
            </div>
            <div class="col-sm-6">
                {{ form_row(form.saleOrder, {'attr': {'readonly': true, 'data-toggle': 'modal', 'data-target': '#sale-order-dialog'} }) }}
                <div class="form-group" id="sale-order-info">
                    <div>
                        <label>Customer:</label>
                        <span data-name="customer-name">{{ receiveOrder.saleOrder.customer.name|default }}</span>
                    </div>
                    <div>
                        <label>Vehicle Brand:</label>
                        <span data-name="sale-order-vehicle-brand">{{ receiveOrder.saleOrder.vehicleBrand|default }}</span>
                    </div>
                    <div>
                        <label>Vehicle Serial Number:</label>
                        <span data-name="sale-order-vehicle-serial-number">{{ receiveOrder.saleOrder.vehicleSerialNumber|default }}</span>
                    </div>
                    <div>
                        <label>Vehicle Color:</label>
                        <span data-name="sale-order-vehicle-color">{{ receiveOrder.saleOrder.vehicleColor|default }}</span>
                    </div>
                    <div>
                        <label>Manufacture Code:</label>
                        <span data-name="vehicle-model-manufacture-code">{{ receiveOrder.saleOrder.vehicleModel.manufactureCode|default }}</span>
                    </div>
                </div>
                {{ form_row(form.serviceBookNumber) }}
            </div>
        </div>
        
        <div>{{ form_row(form.note) }}</div>
        
        <input class="btn btn-primary navbar-inverse" type="submit" value="Save" data-confirm="Do you want to proceed?" />
    
        <div id="sale-order-dialog" class="modal fade" role="dialog">
            <div class="modal-dialog modal-lg" style="width: 90%">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                        <h4 class="modal-title">Sale Order List</h4>
                    </div>
                    <div class="modal-body">
                        <div id="sale-order-grid" data-grid="{{ path('common_sale_order_grid') }}"></div>
                    </div>
                </div>
            </div>
        </div>
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        jQuery(function($) {
            $("#sale-order-grid").on("click", "table[data-role=table] tbody tr", function() {
                $("#sale-order-dialog").modal("hide");
                $("#{{ form.saleOrder.vars.id }}").val($(this).attr("data-field-id"));
                $("[data-name=customer-name]", "#sale-order-info").text($(this).attr("data-field-customer-name"));
                $("[data-name=sale-order-vehicle-brand]", "#sale-order-info").text($(this).attr("data-field-vehicle-brand"));
                $("[data-name=sale-order-vehicle-serial-number]", "#sale-order-info").text($(this).attr("data-field-vehicle-serial-number"));
                $("[data-name=sale-order-vehicle-color]", "#sale-order-info").text($(this).attr("data-field-vehicle-color"));
                $("[data-name=vehicle-model-manufacture-code]", "#sale-order-info").text($(this).attr("data-field-vehicle-model-manufacture-code"));
            });
        });
    </script>
{% endblock %}

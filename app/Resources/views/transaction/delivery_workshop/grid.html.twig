{% set grid_layout = 'datagrid.html.twig' %}

{{ grid_top(grid) }}

<table data-role="table" class="table table-bordered table-striped table-condensed table-hover">
    <thead>
        <tr data-action="sort" class="alert alert-info">
            <th data-group="deliveryWorkshop" data-field="codeNumber">SJ Karoseri #</th>
            <th data-group="deliveryWorkshop" data-field="transactionDate">Tanggal</th>
            <th data-group="deliveryWorkshop" data-field="note">Note</th>
            <th></th>
        </tr>
        <tr data-action="search">
            <th data-group="deliveryWorkshop" data-field="codeNumber"></th>
            <th data-group="deliveryWorkshop" data-field="transactionDate"></th>
            <th data-group="deliveryWorkshop" data-field="note"></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for deliveryWorkshop in grid.data %}
            <tr data-field-id="{{ deliveryWorkshop.id }}">
                <td class="text-left">{{ deliveryWorkshop.codeNumber|default }}</td>
                <td class="text-left">{{ deliveryWorkshop.transactionDate|default ? deliveryWorkshop.transactionDate|date('Y-m-d') }}</td>
                <td class="text-left">{{ deliveryWorkshop.note|default }}</td>
                <td class="text-left">
                    {{ include('transaction/actions.html.twig', {
                        'contents': {
                            '{index}': null,
                            '{new}': null,
                            '{show}': include('action.html.twig', {'url': path('transaction_delivery_workshop_show', {'id': deliveryWorkshop.id}), 'button': 'btn btn-info btn-sm', 'icon': 'glyphicon glyphicon-info-sign'}),
                            '{edit}': include('action.html.twig', {'url': path('transaction_delivery_workshop_edit', {'id': deliveryWorkshop.id}), 'button': 'btn btn-warning btn-sm', 'icon': 'glyphicon glyphicon-edit'}),
                            '{delete}': include('action.html.twig', {'url': path('transaction_delivery_workshop_delete', {'id': deliveryWorkshop.id}), 'button': 'btn btn-danger btn-sm', 'icon': 'glyphicon glyphicon-remove'}),
                        }
                    }) }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4" class="alert alert-warning">
                    <strong>There are no items to display.</strong>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{{ grid_bottom(grid) }}

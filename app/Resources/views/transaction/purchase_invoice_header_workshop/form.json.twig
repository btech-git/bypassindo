{
    "subTotal": "{{ purchaseInvoiceHeader.subTotal|default|number_format(2) }}",
    "taxNominal": "{{ purchaseInvoiceHeader.taxNominal|default|number_format(2) }}",
    "grandTotal": "{{ purchaseInvoiceHeader.grandTotal|default|number_format(2) }}",
    "purchaseInvoiceDetailWorkshops": {
        {%- for index, purchaseInvoiceDetailWorkshop in purchaseInvoiceHeader.purchaseInvoiceDetailWorkshops -%}
            "{{ index }}": {
                "itemName": "{{ purchaseInvoiceDetailWorkshop.itemName|default }}",
                "quantity": "{{ purchaseInvoiceDetailWorkshop.quantity|default }}",
                "unitPrice": "{{ purchaseInvoiceDetailWorkshop.unitPrice|default|number_format(2) }}",
                "discount": "{{ purchaseInvoiceDetailWorkshop.discount|default|number_format(2) }}",
                "total": "{{ purchaseInvoiceDetailWorkshop.total|default|number_format(2) }}"
            }
            {%- if not loop.last -%},{%- endif -%}
        {%- endfor -%}
    }
}
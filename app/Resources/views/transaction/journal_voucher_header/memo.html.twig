{% extends 'base.html.twig' %}

{% block title 'Memo' %}

{% block body %}
    <div class="memo-content">
        <div class="rows-1">
            {{ journalVoucherHeader.codeNumber|default }}
        </div>
        <div class="rows-1">
            {{ journalVoucherHeader.transactionDate|default ? journalVoucherHeader.transactionDate|date('j F Y') }}
        </div>
        <div class="rows-empty">
            &nbsp;
        </div>
        <div>
            <div class="rows-2 price-info content-left">
                aaaaaaaaaaaaaaa aaaaaaaaaaaaa aaaaaaaaaaaaa aaaaaaaaaaaaaa
            </div>
            <div class="rows-2 price-info content-right">
                {{ journalVoucherHeader.totalDebit|default|number_format(2) }}
            </div>
        </div>
        <div>
            <div class="rows-2 price-info content-left">
                bbbbbbbbbbbbbb bbbbbbbbbbbb bbbbbbbbbbb bbbbbbbbbb
            </div>
            <div class="rows-2 price-info content-right">
                {{ journalVoucherHeader.totalCredit|default|number_format(2) }}
            </div>
        </div>
        <div class="rows-3">
            {{ journalVoucherHeader.note|default }}
        </div>
        <div class="rows-2 nominal">
            {{ journalVoucherHeader.totalDebit|default|say|capitalize }} rupiah.
        </div>
    </div>
    
{% endblock %}

{% block stylesheets_include %}
    
{% endblock %}

{% block javascripts_include %}{% endblock %}

{% block stylesheets %}
    <style>
        div.memo-content {
            font-family: Arial;
            font-size: 4mm;
        }
        div.memo-content div {
            margin: 0;
            padding: 0;
            overflow: hidden;
        }
        div.memo-content > div {
            width: 20cm;
        }
        div.memo-content div.content-left, div.memo-content div.content-right {
            vertical-align: top;
            display: inline-block;
        }
        div.memo-content div.rows-1 {
            height: 6mm;
            padding-left: 145mm;
        }
        div.memo-content div.rows-2 {
            height: 12mm;
        }
        div.memo-content div.rows-2.price-info.content-left {
            width: 10cm;
            padding-left: 25mm;
        }
        div.memo-content div.rows-2.price-info.content-right {
            width: 4cm;
            padding-left: 2cm;
        }
        div.memo-content div.rows-2.nominal {
            width: 15cm;
            padding-left: 25mm;
        }
        div.memo-content div.rows-3 {
            width: 15cm;
            height: 18mm;
            padding-left: 25mm;
        }
        div.memo-content div.rows-empty {
            height: 4mm;
        }
    </style>
{% endblock %}
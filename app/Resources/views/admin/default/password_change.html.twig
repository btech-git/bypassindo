{% extends 'default/layout.html.twig' %}

{% block title %}Change Password{% endblock %}

{% block actions %}
    {{ include('action.html.twig', {'url': path('admin_default_profile_show', { 'id': user.id }), 'label': 'Show Profile', 'button': 'btn btn-info', 'icon': 'glyphicon glyphicon-info-sign'}) }}
    {{ include('action.html.twig', {'url': path('admin_default_profile_edit', { 'id': user.id }), 'label': 'Edit Profile', 'button': 'btn btn-warning', 'icon': 'glyphicon glyphicon-edit'}) }}
{% endblock %}

{% block content %}
    {{ form_start(form) }}
        {{ form_row(form.oldPassword) }}
        {{ form_row(form.newPassword) }}
        <input class="btn btn-primary" type="submit" value="Change" />
    {{ form_end(form) }}
{% endblock %}

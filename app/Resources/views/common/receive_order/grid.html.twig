{% set grid_layout = 'datagrid.html.twig' %}

{{ grid_top(grid) }}

<table data-role="table" class="table table-bordered table-striped table-condensed table-hover">
    <thead>
        <tr data-action="sort" class="alert alert-info">
            <th data-group="receiveOrder" data-field="codeNumber">SO #</th>
            <th data-group="receiveOrder" data-field="transactionDate">Transaction Date</th>
            <th data-group="receiveOrder" data-field="vehicleBrand">Vehicle Brand</th>
            <th data-group="receiveOrder" data-field="vehicleType">Vehicle Type</th>
            <th data-group="receiveOrder" data-field="vehicleSerialNumber">Vehicle Serial Number</th>
            <th data-group="receiveOrder" data-field="vehicleProductionYear">Vehicle Production Year</th>
            <th data-group="receiveOrder" data-field="vehicleChassisNumber">Vehicle Chassis Number</th>
            <th data-group="receiveOrder" data-field="vehicleMachineNumber">Vehicle Machine Number</th>
            <th data-group="receiveOrder" data-field="vehicleColor">Vehicle Color</th>
            <th data-group="receiveOrder" data-field="vehicleDescription">Vehicle Description</th>
            <th data-group="customer" data-field="name">Customer Name</th>
        </tr>
        <tr data-action="search">
            <th data-group="receiveOrder" data-field="codeNumber"></th>
            <th data-group="receiveOrder" data-field="transactionDate"></th>
            <th data-group="receiveOrder" data-field="vehicleBrand"></th>
            <th data-group="receiveOrder" data-field="vehicleType"></th>
            <th data-group="receiveOrder" data-field="vehicleSerialNumber"></th>
            <th data-group="receiveOrder" data-field="vehicleProductionYear"></th>
            <th data-group="receiveOrder" data-field="vehicleChassisNumber"></th>
            <th data-group="receiveOrder" data-field="vehicleMachineNumber"></th>
            <th data-group="receiveOrder" data-field="vehicleColor"></th>
            <th data-group="receiveOrder" data-field="vehicleDescription"></th>
            <th data-group="customer" data-field="name"></th>
        </tr>
    </thead>
    <tbody>
        {% for receiveOrder in grid.data %}
            <tr data-field-id="{{ receiveOrder.id }}"
                data-field-customer-name="{{ receiveOrder.saleOrder.customer.name|default }}"
                data-field-sale-order-vehicle-brand="{{ receiveOrder.saleOrder.vehicleBrand|default }}"
                data-field-sale-order-vehicle-type="{{ receiveOrder.saleOrder.vehicleType|default }}"
                data-field-sale-order-vehicle-serial-number="{{ receiveOrder.saleOrder.vehicleSerialNumber|default }}"
                data-field-sale-order-vehicle-serial-number="{{ receiveOrder.saleOrder.vehicleProductionYear|default }}"
                data-field-sale-order-vehicle-serial-number="{{ receiveOrder.saleOrder.vehicleChassisNumber|default }}"
                data-field-sale-order-vehicle-serial-number="{{ receiveOrder.saleOrder.vehicleMachineNumber|default }}"
                data-field-sale-order-vehicle-color="{{ receiveOrder.saleOrder.vehicleColor|default }}">
                data-field-sale-order-vehicle-serial-number="{{ receiveOrder.saleOrder.vehicleDescription|default }}"
                <td class="text-left">{{ receiveOrder.codeNumber|default }}</td>
                <td class="text-left">{{ receiveOrder.transactionDate|default ? receiveOrder.transactionDate|date('Y-m-d') }}</td>
                <td class="text-left">{{ receiveOrder.saleOrder.vehicleBrand|default }}</td>
                <td class="text-left">{{ receiveOrder.saleOrder.vehicleType|default }}</td>
                <td class="text-left">{{ receiveOrder.saleOrder.vehicleSerialNumber|default }}</td>
                <td class="text-left">{{ receiveOrder.saleOrder.vehicleProductionYear|default }}</td>
                <td class="text-left">{{ receiveOrder.saleOrder.vehicleChassisNumber|default }}</td>
                <td class="text-left">{{ receiveOrder.saleOrder.vehicleMachineNumber|default }}</td>
                <td class="text-left">{{ receiveOrder.saleOrder.vehicleColor|default }}</td>
                <td class="text-left">{{ receiveOrder.saleOrder.vehicleDescription|default }}</td>
                <td class="text-left">{{ receiveOrder.saleOrder.customer.name|default }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11" class="alert alert-warning">
                    <strong>There are no items to display.</strong>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{{ grid_bottom(grid) }}

<script>
    jQuery(function($) {
        $("#{{ grid.id }}-top").css("display", "none");
    });
</script>
